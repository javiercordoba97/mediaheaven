<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head") %>
<body>
    <%- include("../partials/header") %>
    <main>
        <h2>Resultados de búsqueda para "<%= query %>"</h2>
        <% if (juegos.length === 0) { %>
            <p>No se encontraron juegos.</p>
        <% } else { %>
            <div class="resultados" style="display: flex; flex-wrap: wrap; gap: 2rem;">
                <% juegos.forEach(juego => { %>
                    <div class="juego-card" style="background: #222; color: #fff; padding: 1rem; border-radius: 12px; width: 220px;">
                        <img src="/img/products/<%= juego.imagen %>" alt="<%= juego.nombre %>" style="width:100%;height:140px;object-fit:cover;border-radius:8px;">
                        <h3 style="margin: 0.5rem 0;"><%= juego.nombre %></h3>
                        <p style="margin: 0.5rem 0;">$ <%= juego.precio %></p>
                        <a href="/product/<%= juego.id %>" style="color:#00baff;text-decoration:underline;">Ver detalle</a>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </main>
    <%- include("../partials/footer") %>
</body>
</html>